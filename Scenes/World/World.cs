using Godot;
using System;

public partial class World : Node2D
{
	/*private PackedScene deadMenu;
	
	public override void _Ready()
	{
		pauseMenu = ResourceLoader.Load<PackedScene>("res://Scenes/PauseMenu/pause_menu.tscn");
	}

	public override void _Process(double delta)
	{
		if(PlayerStats.isDead)
			{
				instance = pauseMenu.Istantiate();
				AddChild(instance);
			}
	}
*/
}
